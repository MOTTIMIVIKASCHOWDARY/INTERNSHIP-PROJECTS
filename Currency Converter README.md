# Currency Converter

A Python program that converts currency using real-time exchange rates from an online API.

## 💡 Features
- Converts between any two currencies
- Uses live exchange rate data
- Easy-to-use terminal interface

## ✅ Requirements
- Python 3
- `requests` module (install using `pip install requests`)

## 🚀 How to Run
1. Open terminal
2. Navigate to the folder:
